# Ponder Indexer Environment Configuration
# Copy this file to .env.local and fill in the values

# =============================================================================
# Chain Selection
# =============================================================================
# Which chain to index: "anvil" | "baseSepolia" | "base"
PONDER_CHAIN=anvil

# =============================================================================
# RPC URLs (provide URL for the chain you're indexing)
# =============================================================================
# Base Mainnet (Chain ID: 8453)
PONDER_RPC_URL_8453=

# Base Sepolia Testnet (Chain ID: 84532)
PONDER_RPC_URL_84532=

# Local Anvil (Chain ID: 31337) - defaults to http://127.0.0.1:8545
PONDER_RPC_URL_31337=http://127.0.0.1:8545

# =============================================================================
# Contract Configuration
# =============================================================================
# World contract address (deployed address)
WORLD_CONTRACT_ADDRESS=

# Start block for indexing (optional, defaults to 0)
# Set this to the deployment block to speed up initial sync
WORLD_START_BLOCK_BASE=
WORLD_START_BLOCK_SEPOLIA=

# =============================================================================
# Database (optional - Ponder uses SQLite by default)
# =============================================================================
# DATABASE_URL=postgresql://user:password@localhost:5432/ponder

# =============================================================================
# Redis (hot ledger used by indexer + game servers)
# =============================================================================
# Standardize on REDIS_URL; keep REDIS_URI as fallback.
REDIS_URL=redis://localhost:6379

